/​opsx:explore​ 
基于当前系统已有能力，定时使用playwright MCP 访问携程页面，LLM 结构化提取，采集结果清洗后双写数据库 + Chroma 向量库，以下是一些具体规则：
- 每天凌晨 2 点自动触发
- 检查数据库中哪些景点数据超过 7 天未更新，只采集过期的部分
- 按优先级排序：严重过期（> 30 天）优先，普通过期（7-30 天）其次
- 采集完成后生成报告并通知
- 请分析项目现状，生成 Spec