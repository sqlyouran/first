```
帮我对景点模块做一次全量测试回归。

1. 先跑后端全部测试：mvn -f backend/pom.xml test
2. 再跑前端全部测试：cd frontend && npm test
3. 列出所有失败的测试，每个标注：
   - 测试文件路径
   - 错误信息
   - 初步判断原因
```

```
针对刚才测试失败的每个用例，逐个分析根因：

- 读测试文件和对应的源文件
- 顺着 import 链找到问题根源
- 分类：是并行开发导致的类型冲突、字段不一致、还是组件重复？
- 给出修复方案（改哪个文件、改什么）

先分析，不要改代码。
```
```
按你刚才的分析，逐个修复。每修一个：

1. 改代码
2. 跑这一个失败的测试确认变绿
3. 再修下一个

不要一次性全改完。

修复时遵循项目现有规则（backend-conventions / frontend-conventions）。
```

```
所有修复完成了。现在跑一遍全量回归：

1. mvn -f backend/pom.xml test
2. cd frontend && npm test

如果还有红灯，继续修复直到全绿。
全绿后贴出最终测试命令和输出。
```