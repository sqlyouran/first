分析 WanderChina 后端的性能瓶颈，重点看：
- JPA 关联查询有没有 N+1 风险
- 哪些高频只读接口适合加缓存
- open-in-view 和游标分页的现状
只做诊断，输出问题清单和优先级，不要改代码

-------------------------------------------------------
帮我给 WanderChina 后端做一次上线前的安全体检，只诊断、不改代码。

我想搞清楚一件事：现在这套服务真要放到公网上，哪些地方会被人钻空子。你把 backend/ 从头到尾过一遍，重点盯下面这几个方向，但不要只盯这几个——只要你觉得有安全风险的地方，都给我列出来。

几个我比较担心的点（举例，不是全部）：

1. 有没有把敏感信息写死在代码或配置里。比如 JWT 的签名密钥、数据库密码，是不是直接硬编码了。。

2. 数据库查询安全。看看有没有手动拼接 SQL 字符串的地方，会不会被 SQL 注入。

3. 越权访问。用户 A 能不能通过改一下 URL 里的 id，就去操作用户 B 的数据（改别人的帖子、删别人的收藏这种）。看看那些"改/删/查自己数据"的接口，有没有校验这条数据到底是不是当前登录用户的。

4. 其它你扫出来的问题。比如鉴权过滤器有没有漏网的接口、异常信息会不会把堆栈/内部细节泄露给前端、有没有敏感字段（密码 hash、salt）不小心序列化返回了、CORS 配得松不松、有没有缺失的输入校验等等。

输出要求：
- 每条给出：问题在哪个文件哪一行、具体是什么问题、真被利用会造成什么后果（用大白话讲）、大概怎么修、严重程度（高/中/低）。
- 也顺便肯定一下已经做对的地方（比如已经有的防护措施），让我知道底子怎么样。
- 按严重程度排个序，高危的放最前面。
- 只出问题清单和诊断，先别动代码。

---------------------------------------------------
帮我给 WanderChina 前端做一次安全体检，只诊断、不改代码。

后端那边我另外查了，这次专门看 frontend/。同样，下面列的是我比较担心的几个点，但你别只查这几个——凡是前端这边可能出安全问题的地方，都给我扫出来。

几个重点方向（举例）：

1. 富文本/用户内容有没有 XSS 风险。项目里帖子、评论这些地方会渲染用户输入的内容。

2. 登录凭证（Token）存哪了、怎么存的。看看 JWT 是放在 localStorage、还是 Cookie，有没有风险项。

3. 其它你扫出来的问题。比如有没有把密钥/敏感配置打进前端 bundle、有没有在 URL 或日志里暴露 token、接口错误信息会不会把敏感内容直接显示给用户等等。

输出要求：
- 每条给出：问题在哪个文件哪一行、具体是什么问题、真被利用会怎样（用大白话讲）、大概怎么修、严重程度（高/中/低）。
- 已经做对的地方也点一下。
- 按严重程度排序，高危在前。
- 只诊断，先别改代码。
