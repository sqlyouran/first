读一遍 `.qoder/rules/` 下的规约，逐条和 `backend/`、`frontend/` 的代码现状对账。重点看：规约里声明的技术栈、命名约定、目录结构，和代码到底一不一致。把漂移点列成一张表。

--------------------------------
我想给 WanderChina 引入测试覆盖率门禁——用 JaCoCo 统计后端覆盖率，再用 git 的 pre-commit hook 卡线：覆盖率不达标就不让 commit。这一步只做调研和方案设计，先别改任何代码、别装任何依赖。

帮我把下面几件事摸清楚：

1. 先搞清楚现在测试是怎么跑的。看看 backend 的 pom.xml，现在用的是什么测试插件（应该是 surefire），有没有 JaCoCo。

2. 引入 JaCoCo 该怎么接。告诉我要在 pom.xml 里加哪个插件、加在什么位置、大概长什么样，跑完 mvn test 之后覆盖率报告会生成在哪。注意别真去改 pom，只告诉我方案。

3. 现在的覆盖率大概是多少——这个最重要。因为我打算把线设在 70%，但我担心存量代码一上来根本到不了 70%，一卡就全红。你评估一下：如果现在全局就卡 70%，现实吗？如果不现实，有没有更稳的做法，比如先只卡核心的 service 层、或者先按现状设一条基线再逐步往上抬。给我一个能落地、不会一上来就把团队卡死的门槛策略。

4. pre-commit hook 具体怎么装。有个关键点你要留意：backend 是一个 git submodule（父仓 first 只存指针，代码在 backend 子仓里）。hook 里大概跑什么命令（跑测试 + 覆盖率检查，不达标就 exit 1 拦下提交），也给我个示例。

5. 顺便帮我想想坑和取舍。比如：每次 commit 都跑全量测试会不会太慢、影响提交体验？有没有办法只在该跑的时候跑？前端要不要也一起纳入（前端是另一个 submodule，用 Vitest）？还有别的我没想到的风险，也一并列出来。

输出我想要的：
- 后端引入 JaCoCo 的具体方案（插件配置示意 + 报告位置）
- 一个务实的覆盖率门槛策略（能不能卡 70%、卡不到怎么循序渐进）
- submodule 场景下 pre-commit hook 的安装方式 + 示例脚本
- 风险和取舍清单

再强调一遍：这一轮只诊断、只出方案，一行代码都不要改。